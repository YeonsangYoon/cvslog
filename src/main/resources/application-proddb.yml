spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DBURL}
    username: ${DBUSERNAME}
    password: ${DBPASSWORD}

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_batch_fetch_size: 500